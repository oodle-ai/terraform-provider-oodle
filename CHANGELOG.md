## 0.1.0 (Unreleased)

FEATURES:

* **New Resource:** `oodle_prompt` manages a GenAI prompt. A
  change publishes a new version and moves the label to it, so
  one definition in Terraform can be applied to every
  deployment.
